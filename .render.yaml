services:
  - type: web
    name: expense-prediction-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn expense_prediction_api_with_lstm_max:app --bind 0.0.0.0:$PORT --workers 1 --threads 8